#include <cstdio>
#include <algorithm>

using namespace std;

int T[51][35001];
int C[35001];

int main() {

    int N, K;
    scanf("%d %d", &N, &K);

    for(int i = 1 ; i <= N; ++i) scanf("%d", &C[i]);

    for(int n = 1 ; n <= N; ++n) {
        for(int k = 1; k <= min(n, K); ++k) {




        }
    }

    return 0;
}